# Git 개념

## 예상 토픽
- Git 객체 모델 (blob/tree/commit/tag)
- 브랜치 = 커밋을 가리키는 포인터
- HEAD / detached HEAD
- merge vs rebase
- fast-forward vs 3-way merge
- 충돌 마커 의미 (`<<<<<<<`, `=======`, `>>>>>>>`)
- reset (`--soft` / `--mixed` / `--hard`)
- revert (vs reset, 언제 어느 것?)
- stash / pop / list
- amend (언제 위험한가)
- GitHub Flow vs Git Flow
- PR/리뷰 문화
- Branch protection rule
- CODEOWNERS

> stub은 자유롭게 추가.
