# 워크스페이스를 git init 했을 경우용
# 각 과제 레포는 독립 git이므로 부모에서 추적하지 않음

# Codyssey 과제 레포 패턴 (B1-N, B2-N, A1-N 등)
B*/
A*/
C*/

# 단, 워크스페이스 자체 자산은 추적
!CLAUDE.md
!INDEX.md
!concepts/
!retrospectives/
!.claude/
!.gitignore

# OS / 에디터
.DS_Store
*.swp
*.tmp
*~

# Claude Code 로컬 설정 (개인 권한)
.claude/settings.local.json

# ODIN 임시 작업 폴더
.outline/

# 환경 변수
.env
.env.local
